import { StepDistDirective } from "./step-dist.directive";

describe("StepDistDirective", () => {
  it("should create an instance", () => {
    const directive = new StepDistDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
