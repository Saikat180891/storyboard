
<ng-template #edit>
              <tr>
                <!-- Sprint -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '70px'}" class="p-center">
                    <app-select [placeholder]="'#'" [options]="options" [value]="data.sprint"></app-select>
                  </div>
                </td>
                <!-- User Story number -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '70px'}" class="p-center">
                    <input class="text-box" type="text" placeholder="Number" value="{{data.us}}">
                  </div>
                </td>
                <!-- User Story name -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '100px'}" class="p-center">
                    <input class="text-box" type="text" placeholder="U/S Name" value="{{data.uname}}">
                  </div>
                </td>
                <!-- Priority -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '70px'}" class="p-center">
                    <app-select [placeholder]="'Low'" [options]="priority" [value]="data.priority"></app-select>
                  </div>
                </td>
                <!-- Rules approved -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '100px'}" class="p-center">
                    <app-select [placeholder]="'No'" [options]="rulesApproved" [value]="data.rules"></app-select>
                  </div>
                </td>
                <!-- Verified test cases -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '100px'}" class="p-center">
                    <app-select [placeholder]="'No'" [options]="rulesApproved" [value]="data.verified"></app-select>
                  </div>
                </td>
                <!-- FTE -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '70px'}" class="p-center">
                    <input class="text-box" type="text" placeholder="Time" value="{{data.fte}}">
                  </div>
                </td>
                <!-- Dev Hrs -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '70px'}" class="p-center">
                    <input class="text-box" type="text" placeholder="Time" value="{{data.devhrs}}">
                  </div>
                </td>
                <!-- Notes -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '100px'}" class="p-center">
                    <input class="text-box" type="text" placeholder="Notes" value="{{data.notes}}" maxlength="100">
                  </div>
                </td>
                <!-- Status -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '100px'}" class="p-center">
                    <app-select [placeholder]="'Status'" [options]="status" [value]="data.status"></app-select>
                  </div>
                </td>
                <!-- Buttons -->
                <td class="table-header p-relative">
                  <div [ngStyle]="{'width': '100%'}" class="p-center flex">
                    <button class="btn btn-flat-blue btn-lg clearfill">SAVE</button>
                    <button class="btn btn-flat-grey btn-sm" (click)="onClose()"><i class="fas fa-times"></i></button>
                  </div>
                </td>
              </tr>
            </ng-template>